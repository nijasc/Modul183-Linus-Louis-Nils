spring:
  application:
    name: linkstack
  datasource:
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    name: ${DB_NAME}
    url: jdbc:postgresql://${DB_HOST}:5173
vaadin:
  launch-browser: false